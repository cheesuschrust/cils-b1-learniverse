
import React from 'react';
import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card';

const Vocabulary = () => {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Vocabulary</CardTitle>
      </CardHeader>
      <CardContent>
        <p>Learn Italian vocabulary.</p>
      </CardContent>
    </Card>
  );
};

export default Vocabulary;
