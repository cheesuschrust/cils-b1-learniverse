
import React from 'react';
import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card';

const Grammar = () => {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Grammar</CardTitle>
      </CardHeader>
      <CardContent>
        <p>Learn Italian grammar.</p>
      </CardContent>
    </Card>
  );
};

export default Grammar;
